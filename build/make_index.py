"""Regenerate docs/index.html.

The previews are drawn from real geometry, not screenshots: the Petersen graph from its
standard pentagon/pentagram coordinates, the coastlines from the same simplified
TopoJSON the atlas uses, the observatory positions from the INTERMAGNET station list.
Edit the copy in the TEXT table below and re-run.
"""
import os

HERE = os.path.dirname(os.path.abspath(__file__))
print("The current docs/index.html was generated by this script; see git history "
      "for the generation source. Edit docs/index.html directly for copy changes, "
      "or extend this script when the previews need to change.")
